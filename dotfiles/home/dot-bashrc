#!/bin/bash

. "$HOME/.profile" # environment

# fuck you
unset all_proxy
unset http_proxy
unset https_proxy
unset ALL_PROXY
unset HTTP_PROXY
unset HTTPS_PROXY

[[ $TERM != *xterm* ]] && [[ $- != *i* ]] && return

export TERM=xterm-256color # fucking

if [[ $BASH_VERSION != *shit* ]]; then 
  PS1='[bash] \u@\h \W $ '
else
  set -WI --mood bash -o emacs
  unset PS1
fi
